import { redirect } from 'next/navigation';

/** Instant demo report — San Antonio ZIP (high GSC traffic, representative PFAS/hardness profile). */
export default function SampleReportPage() {
  redirect('/results/78205');
}
