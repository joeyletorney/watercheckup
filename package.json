{
  "name": "watercheckup",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev -p 3000",
    "dev:poll": "WATCHPACK_POLLING=true next dev -p 3000",
    "clean": "rm -rf .next",
    "dev:clean": "npm run clean && next dev -p 3000",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "^18",
    "react-dom": "^18",
    "recharts": "^2.12.7",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "typescript": "^5"
  }
}
